name: Build FlowMind APK - DEBUG

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-apk:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Latest Stable Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          channel: 'stable'
          cache: false

      - name: Force Create Fresh pubspec.lock
        run: |
          flutter clean
          rm -f pubspec.lock
          rm -rf \~/.pub-cache
          flutter pub get

      - name: DEBUG - Files before regeneration
        run: ls -la

      - name: Completely Regenerate Android Folder (DEBUG)
        run: |
          echo "=== Deleting old android folder ==="
          rm -rf android build .gradle .flutter-plugins* .metadata android.iml
          echo "=== Running flutter create ==="
          flutter create temp_project --platforms=android --org=com.flowmind --project-name flowmind
          echo "=== Copying android folder ==="
          cp -r temp_project/android ./
          echo "=== Deleting temp project ==="
          rm -rf temp_project
          echo "=== Android folder regeneration complete ==="
          ls -la android/

      - name: DEBUG - Flutter Doctor
        run: flutter doctor -v

      - name: Fix flutter_local_notifications Linux Issue
        run: |
          cat >> android/app/build.gradle << 'EOF'

          android {
              packagingOptions {
                  exclude 'META-INF/services/io.flutter.embedding.engine.plugins.PluginRegistrant'
                  pickFirst 'lib/arm64-v8a/libc++_shared.so'
                  pickFirst 'lib/armeabi-v7a/libc++_shared.so'
                  pickFirst 'lib/x86/libc++_shared.so'
                  pickFirst 'lib/x86_64/libc++_shared.so'
              }
          }
          EOF

      - name: Fix Gradle Permission
        run: chmod +x android/gradlew || true

      - name: Build Release APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FlowMind-APK
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30