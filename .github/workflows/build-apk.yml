name: Build FlowMind APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-apk:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter (Force Latest)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.0'     # ← Yeh line sabse important hai
          channel: 'stable'
          cache: false                  # ← Purane cache ko ignore karega

      - name: Fix Gradle permission
        run: chmod +x android/gradlew || true

      - name: Install Dependencies
        run: |
          flutter clean
          flutter pub get

      - name: Build Release APK
        run: |
          flutter build apk --release --no-tree-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FlowMind-APK
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: Show APK Size
        run: ls -lh build/app/outputs/flutter-apk/app-release.apk